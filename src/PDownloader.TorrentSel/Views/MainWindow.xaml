<ui:FluentWindow x:Class="PDownloader.TorrentSel.Views.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:local="clr-namespace:PDownloader.TorrentSel.Views"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:utils="clr-namespace:PDownloader.TorrentSel.Utils"
                 Title="{utils:Localization torrent_selector_app_title}"
                 Width="720"
                 Height="570"
                 MinWidth="600"
                 MinHeight="440"
                 WindowStartupLocation="CenterScreen"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Tabbed"
                 WindowCornerPreference="Round"
                 Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 d:DataContext="{d:DesignInstance local:MainWindow,
                                   IsDesignTimeCreatable=False}"
                 mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"
                     Title="{utils:Localization torrent_selector_app_title}"
                     CloseWindowByDoubleClickOnIcon="True">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/app-256.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Grid.Row="1" Margin="24,16,24,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <ui:TextBlock Text="{Binding ViewModel.TorrentName}"
                              FontSize="20"
                              FontWeight="SemiBold"
                              TextTrimming="CharacterEllipsis" />
                <ui:TextBlock Text="{utils:Localization torrent_selector_description}"
                              Margin="0,4,12,0"
                              Opacity="0.62"
                              TextWrapping="Wrap" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                <ui:Button Content="{utils:Localization torrent_selector_select_all}"
                           Command="{Binding ViewModel.SelectAllCommand}" />
                <ui:Button Content="{utils:Localization torrent_selector_clear}"
                           Margin="8,0,0,0"
                           Command="{Binding ViewModel.SelectNoneCommand}" />
            </StackPanel>
        </Grid>

        <ListBox Grid.Row="2"
                 Margin="20,0,20,12"
                 Padding="0,4"
                 ItemsSource="{Binding ViewModel.Files}"
                 Background="Transparent"
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="4,3" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Focusable" Value="False" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="ItemBorder"
                                        Padding="14,10"
                                        Background="{DynamicResource ControlFillColorDefaultBrush}"
                                        BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        CornerRadius="{DynamicResource ControlCornerRadius}">
                                    <ContentPresenter />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ItemBorder"
                                                Property="Background"
                                                Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <CheckBox Grid.Column="0"
                                  Margin="0,0,12,0"
                                  VerticalAlignment="Center"
                                  IsChecked="{Binding IsSelected, Mode=TwoWay}" />
                        <StackPanel Grid.Column="1">
                            <ui:TextBlock Text="{Binding FileName}"
                                          FontWeight="SemiBold"
                                          TextTrimming="CharacterEllipsis" />
                            <ui:TextBlock Text="{Binding RelativePath}"
                                          Margin="0,3,12,0"
                                          FontSize="11"
                                          Opacity="0.58"
                                          TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                        <ui:TextBlock Grid.Column="2"
                                      Text="{Binding SizeText}"
                                      VerticalAlignment="Center"
                                      Opacity="0.7" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Border Grid.Row="3"
                Padding="24,14"
                BorderThickness="0,1,0,0"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <ui:TextBlock Text="{Binding ViewModel.SelectionSummary}"
                                  FontWeight="SemiBold" />
                    <ui:TextBlock Text="{Binding ViewModel.ErrorMessage}"
                                  Margin="0,3,12,0"
                                  Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                                  TextWrapping="Wrap" />
                </StackPanel>
                <ui:Button Grid.Column="1"
                           MinWidth="122"
                           Appearance="Primary"
                           Content="{utils:Localization torrent_selector_continue}"
                           Icon="{ui:SymbolIcon ArrowDownload24}"
                           Command="{Binding ViewModel.ConfirmCommand}" />
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>
